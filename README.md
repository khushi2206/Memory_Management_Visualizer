# Memory Management Visualizer

React + TypeScript + Vite app for visualizing paging (FIFO, LRU, Optimal), comparison mode, history log, and Q&amp;As.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
